{
  "name": "workshop",
  "projectType": "library",
  "tags": ["type:docs"],
  "targets": {
    "preview": {
      "command": "./preview_build",
      "options": {
        "cwd": "packages/workshop"
      },
      "cache": false
    },
    "deploy": {
      "command": "git branch -D workshop-deploy 2>/dev/null; git subtree split --prefix=packages/workshop -b workshop-deploy && git push workshopstudio workshop-deploy:main",
      "cache": false
    }
  }
}
