{
  "name": "workshop",
  "projectType": "library",
  "tags": ["type:docs"],
  "targets": {
    "preview": {
      "command": "./preview_build",
      "options": {
        "cwd": "packages/workshop"
      },
      "cache": false
    },
    "deploy": {
      "command": "git subtree split --prefix=packages/workshop -B workshop-deploy && git push workshopstudio workshop-deploy:main",
      "cache": false
    }
  }
}
