{
  "name": "prd-writer",
  "projectType": "application",
  "sourceRoot": "packages/prd-writer/src",
  "tags": ["type:app", "lang:python"],
  "targets": {
    "run": {
      "command": "uv run python -m prd-writer.main",
      "options": {
        "cwd": "packages/prd-writer/src"
      }
    },
    "install": {
      "command": "uv sync",
      "options": {
        "cwd": "packages/prd-writer"
      },
      "cache": true,
      "inputs": ["{projectRoot}/pyproject.toml", "{projectRoot}/uv.lock"],
      "outputs": ["{projectRoot}/.venv"]
    },
    "lint": {
      "command": "uv run ruff check src/",
      "options": {
        "cwd": "packages/prd-writer"
      },
      "cache": true,
      "inputs": ["default"],
      "dependsOn": ["install"]
    },
    "typecheck": {
      "command": "uv run pyright src/",
      "options": {
        "cwd": "packages/prd-writer"
      },
      "cache": true,
      "inputs": ["default"],
      "dependsOn": ["install"]
    }
  }
}
