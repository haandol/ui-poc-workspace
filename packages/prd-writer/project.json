{
  "name": "prd-writer",
  "projectType": "application",
  "sourceRoot": "packages/prd-writer/src",
  "tags": ["type:app", "lang:typescript"],
  "targets": {
    "build": {
      "command": "tsc && cp -r src/templates src/guides dist/",
      "options": {
        "cwd": "packages/prd-writer"
      },
      "cache": true,
      "inputs": ["default"],
      "outputs": ["{projectRoot}/dist"]
    },
    "dev": {
      "command": "tsx src/index.ts",
      "options": {
        "cwd": "packages/prd-writer"
      }
    },
    "start": {
      "command": "node dist/index.js",
      "options": {
        "cwd": "packages/prd-writer"
      },
      "dependsOn": ["build"]
    }
  }
}
