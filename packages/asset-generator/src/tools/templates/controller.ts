import { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js'
import type { DIContainer } from '../../di/container.js'

export function registerTemplateTools(_server: McpServer, _container: DIContainer): void {
  // TODO: Register asset generation MCP tools
}
